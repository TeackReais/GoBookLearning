package main

/*import (
	"bytes"
	"io"
	"os"
)*/

//interface类型的使用
func main() {
	/* 	var w io.Writer //interface
	   	w = os.Stdout   //结构体实例分配
	   	w = new(bytes.Buffer)
	   	var rwc io.ReadWriteCloser
	   	rwc = os.Stdout*/ 
}
